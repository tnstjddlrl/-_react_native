import React, { useRef, useState } from 'react';
import {
    SafeAreaView,
    ScrollView,
    StatusBar,
    StyleSheet,
    Text,
    useColorScheme,
    View,
    Dimensions,
    TouchableWithoutFeedback,
    TouchableOpacity
} from 'react-native';

const ScrollMain = () => {
    return (
        <View>
            <Text>스크롤 테스트</Text>
        </View>
    )
}

export default ScrollMain